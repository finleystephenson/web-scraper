---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app.py, requirements.txt, templates/index.html]
autonomous: true
---

<objective>
Create Flask app structure with URL input form and clean Tailwind UI.

Purpose: Establish the foundation for the scraper - a working Flask app that accepts URLs and displays a placeholder for results.
Output: Running Flask app with form submission working.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Flask app with routes</name>
  <files>app.py, requirements.txt</files>
  <action>
Create requirements.txt with:
- flask
- requests
- beautifulsoup4
- gunicorn

Create app.py with:
- Flask app instance
- GET route "/" that renders index.html template
- POST route "/scan" that accepts URL from form, returns placeholder result for now (just echo the URL back)
- Use render_template for HTML, request.form for POST data

Keep it minimal - scraping logic comes in Phase 2.
  </action>
  <verify>python -c "from app import app; print('Import OK')" succeeds</verify>
  <done>Flask app imports without errors, has / and /scan routes defined</done>
</task>

<task type="auto">
  <name>Task 2: Create HTML template with form and Tailwind</name>
  <files>templates/index.html</files>
  <action>
Create templates/ directory and index.html with:
- Tailwind CSS via CDN (no build step)
- Clean centered layout with "Lead X-Ray" heading
- Form with URL input field (type="url", required) and "Scan" button
- POST to /scan
- Results section that shows:
  - Placeholder text when no scan done
  - URL scanned (passed from Flask via template variable)
  - Placeholder for SEO vitals section (title, H1, meta description)
  - Placeholder for contact info section (emails, phones, socials)
  - "No contact info detected" message when appropriate
- Use Tailwind classes for clean, professional look:
  - Max-width container, centered
  - Card-style form area
  - Blue primary button
  - Responsive design
  </action>
  <verify>Open http://localhost:5000 in browser, form displays correctly</verify>
  <done>HTML template renders with form, Tailwind styling applied, form submits to /scan</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -r requirements.txt` succeeds
- [ ] `python -c "from app import app"` imports without error
- [ ] `flask run` starts server without errors
- [ ] Browser shows styled form at http://localhost:5000
- [ ] Form submission to /scan works (returns placeholder)
</verification>

<success_criteria>
- All tasks completed
- Flask app runs locally
- Form displays with Tailwind styling
- Form submission works (placeholder response OK)
- No Python errors or import issues
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
